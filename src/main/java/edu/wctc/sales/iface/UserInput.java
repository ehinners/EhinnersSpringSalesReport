package edu.wctc.sales.iface;

public interface UserInput
{
    // Currently unused interface for user input.
    // This would be implemented if the program wanted to allow direct input of each individual order.
    // This would be implemented if the program wanted to allow the user direct control over which file is read.
    String getInput(String prompt);
}
